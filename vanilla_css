/* Root Variables for Global Styles */
:root {
    --body_background: url('https://d2p5uoj6rqd2yg.cloudfront.net/pexels-laura-penwell-309923-3608056.jpg');
    --main_text: #ffffff; /* White text color for contrast */
    --padding_top_main: 60px;
    --global_box_shadow: 0 .125rem .375rem 0 rgba(0, 0, 0, 0.12);
    --font_body: "Verdana", sans-serif;
    --font_heading: "Verdana", sans-serif;
    --primary_color: #1dbf72;
}

/* General Body Styling */
body {
    background: var(--body_background) no-repeat center center fixed;
    background-size: cover;
    font-family: var(--font_body);
    color: var(--main_text);
    margin: 0;
    padding: 0;
}

/* Header Styling - Dark Transparent and Blurry */
#header {
    background-color: rgba(0, 0, 0, 0.7); /* Dark transparent background */
    backdrop-filter: blur(5px); /* More pronounced blur effect */
    box-shadow: var(--global_box_shadow); /* Light shadow for depth */
    padding: 20px 40px;
    position: absolute;
    width: 100%;
    z-index: 10;
}

/* Header Text and Links */
#header, #header .navbar-brand, #header .nav-item, #header a {
    color: var(--main_text); /* White text for readability */
    text-transform: uppercase;
    font-weight: 600;
}

#header .nav-item {
    margin-right: 25px;
}

#header .navbar-brand {
    display: flex;
    align-items: center;
}

/* Hide the original logo SVG */
#header .navbar-brand svg {
    display: none;
}

/* Display the favicon as logo, aligned properly */
#header .navbar-brand::before {
    content: '';
    display: inline-block;
    width: 30px;
    height: 30px;
    background-image: url('https://public-blob.s3.us-east-1.amazonaws.com/longstraw-carbon-favicon-color.png');
    background-size: contain;
    background-repeat: no-repeat;
    margin-right: 10px;
    vertical-align: middle; /* Align logo vertically with the text */
}

/* Navbar Links Styling */
#header .navbar-nav a {
    color: #fff;
    font-size: 18px;
    padding: 10px 15px;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease, color 0.3s ease;
}

#header .navbar-nav a:hover {
    background-color: var(--primary_color);
    color: #ffffff;
}

/* Hero Section */
.hero {
    padding: var(--padding_top_main) 20px;
    text-align: center;
    color: var(--main_text);
    position: relative;
    z-index: 5;
    min-height: 70vh;
    display: flex;
    justify-content: center;
    flex-direction: column;
    align-items: center;
    text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.5);
}

.hero h1 {
    font-size: 48px;
    font-weight: 700;
    margin-bottom: 20px;
    text-transform: uppercase;
}

.hero p {
    font-size: 20px;
    font-weight: 300;
    margin-bottom: 40px;
    max-width: 800px;
}

.hero .cta-btn {
    background-color: var(--primary_color);
    color: #ffffff;
    border: none;
    padding: 12px 30px;
    border-radius: 50px;
    font-size: 18px;
    text-transform: uppercase;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.2s ease;
}

.hero .cta-btn:hover {
    background-color: #17a45c;
    transform: scale(1.05);
}

/* Feature Section */
.features {
    display: flex;
    justify-content: space-around;
    padding: 80px 20px;
    background-color: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(5px);
}

.features .feature {
    width: 30%;
    text-align: center;
    color: var(--main_text);
    padding: 20px;
}

.features .feature img {
    width: 60px;
    margin-bottom: 20px;
    transition: transform 0.3s ease;
}

.features .feature img:hover {
    transform: scale(1.1);
}

.features .feature h3 {
    font-size: 22px;
    font-weight: 600;
    margin-bottom: 10px;
}

.features .feature p {
    font-size: 16px;
    color: #ccc;
}

/* Footer Styling */
footer {
    background-color: rgba(0, 0, 0, 0.7);
    color: #fff;
    padding: 40px 20px;
    text-align: center;
    position: relative;
}

footer .footer-links a {
    color: #fff;
    text-decoration: none;
    margin: 0 15px;
    font-size: 16px;
    transition: color 0.3s ease;
}

footer .footer-links a:hover {
    color: var(--primary_color);
}

footer .social-links a {
    color: #fff;
    margin: 0 10px;
    font-size: 18px;
    transition: color 0.3s ease;
}

footer .social-links a:hover {
    color: var(--primary_color);
}

footer p {
    font-size: 14px;
    color: #ccc;
    margin-top: 20px;
}

/* Responsive Design */
@media (max-width: 768px) {
    #header .navbar-nav {
        display: none;
    }

    .hero h1 {
        font-size: 36px;
    }

    .features {
        flex-direction: column;
        align-items: center;
    }

    .features .feature {
        width: 80%;
        margin-bottom: 40px;
    }

    footer .footer-links a,
    footer .social-links a {
        font-size: 14px;
    }
}
